Sections~\ref{sec:hybrid-scan} and~\ref{subsec:fpr-guarantee} have
already told us \emph{when} Seg-ARE is compact: when the input has
dense regions separated by gaps the forward sweep can isolate.
Below we work through two examples and count the bits.

Grafite, when the requested $\varepsilon$ falls below
the lossless threshold $u/n$, degenerates into a single Elias--Fano (EF) encoding of
the raw sorted keys --- the wrapper substitutes
\texttt{ef\_sux\_vector(keys)} and the filter answers range queries by
predecessor probe (Section~\ref{subsec:grafite-fallback}).
Below this threshold Grafite is an approximate filter; above it, it is
essentially EF over the whole universe --- the same exact/approximate
switch as our Adaptive Filter (Section~\ref{sec:adaptive}).

Seg-ARE applies the same EF primitive, but only after segmenting the
input.
Each dense cluster identified by the forward sweep is handed to
an exact-mode ERE sub-filter (Section~\ref{sec:adaptive}), which
encodes the cluster's keys locally; sparse keys outside any cluster
fall through to the truncation hash (Section~\ref{sec:truncation}).
The total size of a Seg-ARE filter is the sum of per-cluster EF
encodings plus the fallback truncation filter --- versus Grafite's
single EF encoding over the whole universe.

This framing makes the win condition concrete: Seg-ARE beats Grafite
exactly when the per-cluster EF plus the fallback together cost less
than one universe-wide EF.
The bpk gap then decomposes as
\[
    \underbrace{\log_2(U_{\text{global}}/n)}_{\text{Grafite EF data}}
    \;-\;
    \underbrace{\Bigl(\sum_{c} \frac{n_c}{n} \cdot
    \log_2\!\bigl(2^{M_c}/n_c\bigr) \;+\; \frac{n_{\text{fb}}}{n} \cdot
    \log_2(1/\varepsilon)\Bigr)}_{\text{Seg-ARE: EF data + truncation fallback}},
\]
where $U_{\text{global}}$ is the spread of the whole input, $2^{M_c}$
and $n_c$ are the local window width and key count of the $c$-th
cluster, and $n_{\text{fb}}$ is the number of keys handed to the
fallback.

The two paragraphs below work through two examples: a synthetic
clustered distribution and the SOSD-Wikipedia timestamps.

\paragraph{Clustered synthetic ($n = 2^{28}$).}
The clustered generator places keys across essentially the full
$2^{64}$ universe (see Section~\ref{sec:eval-setup}), so
$\log_2(U/n) = 36$ bits per key for the universe-wide baseline.

The forward sweep isolates $8911$ dense clusters covering $72.9\%$
of the keys, with $M_c \in [14.6,\, 28.7]$ bits and
$n_c \in [512,\, 6.7\!\times\!10^{7}]$.
The remaining $27.1\%$ fall through to the truncation hash.
The key-weighted per-cluster EF \emph{lower-part} contribution is
\[
    \frac{1}{\sum_c n_c} \sum_c n_c \cdot \log_2\!\Bigl(\frac{2^{M_c}}{n_c}\Bigr)
    \;\approx\; 2.87 \text{ bits per in-cluster key}
\]
--- the remaining ${\approx}\,7$ bits per key come from metadata overhead
and the truncation-fallback keys, giving ${\approx}\,10$ bits per key in total.

Grafite cannot trigger its lossless fallback
(Section~\ref{subsec:grafite-fallback}) at the requested $\varepsilon$
either: lossless requires $n\mathcal{L}/\varepsilon > U$, i.e.\
$\varepsilon < n\mathcal{L}/U = 2^{28}\!\cdot\!128/2^{64} \approx
1.9\!\times\!10^{-9}$.
At any $\varepsilon$ used in this evaluation Grafite stays in approximate
mode and follows the Goswami bound $\log_2(\mathcal{L}/\varepsilon) + O(1)$.
Universe-wide lossless EF on this input would saturate at
${\approx}\,38.9$~BPK --- not a useful operating point for a filter
whose whole purpose is to be much smaller than the keys themselves.

Figure~\ref{fig:seg-clustered-L128} shows the empirical curve at
query length $L = 128$.
At matched $\mathrm{FPR} \approx 6\!\times\!10^{-5}$, Seg-ARE sits at $9.77$ BPK
and Grafite at $20.63$ BPK. Both filters then drop below the
$4\!\times\!10^{-6}$ measurement floor (set by the $10^{6}$-query
batch): Seg-ARE at $10.07$ BPK and Grafite at $26.63$ BPK; the true FPR at these points
is non-zero but below detection, not exactly zero.
The $16.5$-BPK gap at the floor is therefore a lower bound on the BPK separation:
matching FPRs deeper would widen the gap further, with Grafite's
genuinely zero-FPR point only at the $38.94$~BPK lossless saturation.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{seg_vs_grafite_clustered_L128}
    \caption{FPR vs BPK on the synthetic clustered distribution
        ($n = 2^{28}$, query length $L = 128$)}
    \label{fig:seg-clustered-L128}
\end{figure}

\FloatBarrier

\paragraph{SOSD-Wikipedia ($n = 2^{28}$).}
Wikipedia edit timestamps form a tight near-sequential cluster:
after deduplication $n \approx 9 \times 10^{7}$ keys span a window
of width $\max - \min \approx 2.37\!\times\!10^{8}$ (about $29$
bits, see Table~\ref{tab:sosd-gaps}).
The segmenter does not collapse the
input into a single cluster: the forward sweep finds $359$ dense
clusters covering $99.9\%$ of the keys, with one giant cluster of
$7.7\!\times\!10^{7}$ keys ($M_c = 26.5$ bits) and $358$ smaller
ones ($M_c$ down to $14.6$ bits).
The full Seg-ARE filter uses 3.74 bits per key.

Grafite, on the same input, encodes the raw 29 bits universe.
The EF coding saves $\approx \log_2(n)$ bits per key, that gives $29 - \log_2(9 \times 10^{7}) \approx 29 - 26.4 = 2.6$ bits per key.
Together with the metadata overhead, it reaches FPR$\,=0$ at $4.6$~BPK in Figure~\ref{fig:seg-wiki-L128}.
The $0.9$-BPK gap to Seg-ARE is the normalization and segmentation saving --- both
filters use the same EF coding primitive.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{seg_vs_grafite_wiki_L128}
    \caption{FPR vs BPK on SOSD-Wikipedia ($n = 2^{28}$, query length $L = 128$)}
    \label{fig:seg-wiki-L128}
\end{figure}

\FloatBarrier

\paragraph{Uniform synthetic.}
Uniform is the case where Seg-ARE has no clusters to extract --- every
key goes to the truncation fallback. The truncation scheme
(Section~\ref{sec:truncation}) pays
$\log_2(1/\varepsilon) + O(1)$ bits per key \emph{independently of
    $\mathcal{L}$}: large inter-key gaps make collisions rare,
so the fixed-suffix length needed to hit a target $\varepsilon$ does
not have to grow with the query length.
Grafite has no such mechanism and follows the Goswami bound $\log_2(\mathcal{L}/\varepsilon) + O(1)$,
which grows with $\mathcal{L}$ by exactly $\log_2 \mathcal{L}$ bits.
Table~\ref{tab:bpk} (Uniform / gap-heavy column)
shows the consequence at $\varepsilon = 10^{-3}$: at
$\mathcal{L} = 1$ both filters sit at $12.4$~BPK, at
$\mathcal{L} = 16$ Seg-ARE is $15.1$~BPK against Grafite's
$16.6$~BPK, and at $\mathcal{L} = 1024$ Grafite drops out of the
measured range while Seg-ARE still reaches the target at
$15.3$~BPK. The widening gap is the missing $\log_2 \mathcal{L}$
factor.

\FloatBarrier