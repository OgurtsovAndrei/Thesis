\section{Succinct Representation (SODA 2015, §3.2)}
\label{sec:ere}

The inner layer of every approximate filter is an \emph{Exact Range Emptiness} (ERE)
structure: given a universe $U'$ of $K$-bit keys
and a set $S' \subseteq U'$ of $n$ keys, it answers
``$[a, b] \cap S' = \emptyset$?'' exactly (no false positives or false negatives).

\begin{remark}
    Throughout this chapter, $S'$ denotes the input to ERE; in the ARE pipeline (Chapter~\ref{chap:hash}), $S' = h(S)$.
\end{remark}

\subsection{High-Level Structure}
\label{subsec:ere-high-level-structure}

To achieve $n \cdot (K - \log_2(n) + O(1)) = n \log_2(r/n) + O(n)$ bits~\citep{goswami2015approximate},
the structure divides the universe $U'$ into $B = 2^{\lfloor \log_2 n \rfloor}$ equal-sized blocks.
Conceptually, ERE has two levels: a succinct navigation layer over blocks, and a compact local representation of the keys.

All keys inside one block have a common $\log_2 B = \lfloor \log_2 n \rfloor$ bits prefix.
Therefore, key suffixes can be stored in the array with $w = K - \lfloor \log_2 n \rfloor$ bits per key.
The succinct navigation layer is used to determine block boundaries.

\subsection{Original ERE Structure}
\label{subsec:ere-original-structure}

We now describe the original ERE construction of \citet{goswami2015approximate}
more precisely.
It consists of the following parts:

\begin{enumerate}
    \item $D_1$ succinct bit vector: stores block occupancy: $D_1[i] = 1$ iff block $i$ contains at least one key.
    \item $D_2$ succinct bit vector: encodes the sizes of non-empty blocks in unary form.
    If a non-empty block $i$ contains $n_i$ keys, then it contributes a $1$ followed by $n_i$ zeros.
    \item $A_{ds}$ the array of structures: each data structure consists of:
    \subitem The sorted list of suffixes
    \subitem A Weak Prefix Search index over those suffixes
    (Section~\ref{sec:wps}).
\end{enumerate}

A query range is split into at most three parts: a sequence of complete blocks
and up to two partially covered boundary blocks.
The complete-block part is answered by the global succinct layer using $D_1$.
The boundary parts are answered by locating the corresponding local
structure in $A_{ds}$ via $D_1$ and $D_2$, and then checking emptiness
inside the block.
The local check uses two weak prefix queries (Section~\ref{sec:wps})
as an $O(1)$ index into the bucket's sorted suffix list; the stored
suffixes themselves witness (non-)emptiness via a final comparison.
\colorbox{orange}{TODO: btw i do not like forward refs, maybe I need to chenge order of sections?}

\subsection{Local Search in Buckets}
\label{subsec:ere-local-search}

The original paper~\citep{goswami2015approximate} suggests a Weak Prefix Search
(WPS)~\citep{belazzougui2010fast} structure based on a Hollow Z-Fast Trie
(ZFT)~\citep{belazzougui2009monotone} for $O(1)$ worst-case local queries.
We use binary search on bit-packed suffixes, with an adaptive fallback to linear scan for small buckets, instead.
At the bucket sizes that occur in practice, binary search --- or even
linear scan on the smallest buckets --- is faster than an $O(1)$ ZFT
lookup, whose hidden constant factor is large, and additionally avoids
the ZFT's metadata overhead.
Even the worst-case bucket of $k^*$ keys takes ${\sim}50$--$55$~bytes
at $K = 64$ (Table~\ref{tab:ere-bucket-stats}), fitting in a single
cache line, so a linear scan reduces to one sequential memory access.
Details are given in Section~\ref{sec:wps}.

\subsection{One-Vector Optimization}
\label{subsec:ere-one-vector}

In the optimized variant, we simplify the navigation layer.
Instead of storing occupancy and non-empty block offsets separately in $D_1$ and $D_2$, we replace them with a single succinct bit-vector $D$.
Every block $i$ with $n_i$ keys is encoded as a single $1$ followed by $n_i$ zeros, whether the block is empty or not.
In particular, an empty block (with $n_i = 0$) is encoded by a single $1$.
One final sentinel $1$ is appended at the end.

\paragraph{Effect on space and navigation.}
Let $M$ denote the number of non-empty blocks.
In the original layout, the metadata size is
\[
    |D_1| + |D_2| = B + (n + M + 1).
\]
In the optimized layout, the metadata becomes
\[
    |D| = B + n + 1.
\]
The exact metadata reduction is $M$ bits.
Operationally, this means that the old two-stage lookup through $D_1$
and $D_2$ is replaced by direct navigation via \textsc{Select} on $D$.

\paragraph{Metadata footprint on uniform input.}
Table~\ref{tab:ere-metadata-bpk} reports the metadata BPK on $n$
uniform 64-bit keys for $n \in \{2^{20},\ 2^{24},\ 2^{28}\}$.
The information-theoretic delta is ${\sim}0.63$~BPK, matching the
analytic prediction $M/n \to 1 - e^{-1} \approx 0.632$
(each block is independently empty with probability $e^{-1}$,
Section~\ref{subsec:block-sizes-and-lookup-strategy}).
The physical delta is ${\sim}0.80$~BPK, amplified by a constant
${\sim}26\%$ overhead from the rank/select index --- intrinsic to
succinct bit vectors and required for the $O(1)$ navigation
guarantees.
Both deltas are stable across $n$.

\begin{table}[h]
    \centering
    \begin{tabular}{@{}rcc@{\hskip 24pt}cc@{}}
        \toprule
        & \multicolumn{2}{c}{\textbf{Num} (logical bits)} & \multicolumn{2}{c}{\textbf{Alloc} (with rank/select index)} \\
        \cmidrule(r){2-3} \cmidrule(l){4-5}
        $n$      & two-vector & one-vector & two-vector & one-vector \\
        \midrule
        $2^{20}$ & $2.63$     & $2.00$     & $3.33$     & $2.53$     \\
        $2^{24}$ & $2.63$     & $2.00$     & $3.33$     & $2.53$     \\
        $2^{28}$ & $2.63$     & $2.00$     & $3.33$     & $2.53$     \\
        \midrule
        $\Delta$ & \multicolumn{2}{c}{$-0.63$ BPK ($-24\%$)} & \multicolumn{2}{c}{$-0.80$ BPK ($-24\%$)} \\
        \bottomrule
    \end{tabular}
    \caption{Metadata BPK for the two-vector $(D_1, D_2)$ layout
        (Section~\ref{subsec:ere-original-structure}) versus the
        one-vector $D$ layout, on $n$ uniform 64-bit keys.
        \emph{Num}: logical bitvector length ($D_1.\textsc{num()} + D_2.\textsc{num()}$
        vs.\ $D.\textsc{num()}$); matches the analytic
        $|D_1|+|D_2| = B + (n + M + 1)$ and $|D| = B + n + 1$ formulas above.
        \emph{Alloc}: physical succinct-bit-vector footprint (bits plus
        pointer, rank, and select indices). The suffix array $A_{ds}$ is
        excluded --- identical in both layouts.}
    \label{tab:ere-metadata-bpk}
\end{table}

Against the $\sim 20$~BPK total ERE footprint --- dominated by the
suffix array $A_{ds}$, which is identical between the two layouts ---
the absolute saving of $0.80$~BPK looks small.
But it is a $\sim 24\%$ reduction in metadata: the only part that
can shrink without information loss.
Cross-distribution measurements are deferred to
Chapter~\ref{chap:evaluation}
(Table~\ref{tab:ere-backend-cross-distribution}).

\paragraph{Effect on query operation count.}
The single-vector layout does not strictly reduce the number of \textsc{Rank}/\textsc{Select} calls in every case: the original two-vector layout could short-circuit empty boundary blocks via a $D_1.\textsc{bit()}$ probe, while the new layout always pays at least two \textsc{Select}$(D)$ per boundary block.
However, in the two most common ARE query patterns --- short ranges that hit a single non-empty block, or that span two adjacent non-empty blocks --- the new layout saves \emph{one} and \emph{three} \textsc{Rank}/\textsc{Select} calls per query, respectively, with long ranges that scan both boundary buckets saving up to four.
The structural advantage is that every \textsc{Rank}/\textsc{Select} now hits one contiguous succinct bit vector $D$ instead of alternating between $D_1$ and $D_2$, so the auxiliary index pages and cache lines loaded by the first call are likely to serve subsequent calls in the same query.
The full per-case breakdown is given in Table~\ref{tab:ere-rank-select};
end-to-end query latency on uniform and clustered data is reported in
Chapter~\ref{chap:evaluation}
(Table~\ref{tab:ere-backend-query-latency}).

\begin{table}[h]
    \centering
    \begin{tabular}{@{}lccrl@{}}
        \toprule
        Query type                            & $(D_1, D_2)$ ops  & $D$ ops    & $\Delta$      & Note                             \\
        \midrule
        \textbf{Same block, non-empty}        & $1\,R + 2\,S = 3$ & $2\,S = 2$ & $\mathbf{-1}$ & \textbf{Most frequent, hot path} \\
        Same block, empty                     & $0$               & $2\,S = 2$ & $+2$          & Cold-path regression             \\
        \textbf{Adjacent, both non-empty}     & $2\,R + 4\,S = 6$ & $3\,S = 3$ & $\mathbf{-3}$ & \textbf{Most frequent, hot path} \\
        Adjacent, both empty                  & $0$               & $3\,S = 3$ & $+3$          & Cold-path regression             \\
        Long range, both boundaries non-empty & $4\,R + 4\,S = 8$ & $4\,S = 4$ & $-4$          & Op count halved                  \\
        Long range, intermediate non-empty    & $2\,R = 2$        & $4\,S = 4$ & $+2$          & More ops, single vector          \\
        Long range, all empty                 & $2\,R = 2$        & $4\,S = 4$ & $+2$          & Cold-path regression             \\
        \bottomrule
    \end{tabular}
    \caption{Number of \textsc{Rank}/\textsc{Select} operations per query case for the
    original two-vector $(D_1, D_2)$ layout (Section~\ref{subsec:ere-original-structure})
        versus the one-vector $D$ layout (Section~\ref{subsec:ere-one-vector}).
        $R$ denotes \textsc{Rank} on $D_1$; $S$ denotes \textsc{Select} on $D_2$ or $D$ as
        appropriate. Bolded rows are the dominant query patterns in typical ARE workloads.
        \textsc{Bit} probes and bucket-scan operations are not counted (identical between
        layouts).}
    \label{tab:ere-rank-select}
\end{table}

\colorbox{orange}{TODO: write about better mem layout}

\subsection{Block sizes and Lookup strategy}
\label{subsec:block-sizes-and-lookup-strategy}

\paragraph{Block occupancy under uniform distribution.}
The Exact Range Emptiness structure divides the universe into $B = 2^{\lfloor \log_2 n \rfloor}$ blocks;
each non-empty block stores its keys' suffixes in a packed array (a \emph{bucket}).
The expected
number of keys per block is $\lambda = n/B \in [1,\,2)$.
When $n$ is a power of two, $B = n$ and $\lambda = 1$ exactly.
Each key falls into a uniformly random block, so the occupancy of a fixed block
follows $\mathrm{Bin}(n,\,1/B)$, which converges to $\mathrm{Poisson}(\lambda)$
as $n \to \infty$~\citep[Ch.~5]{mitzenmacher2017probability}.

For $\lambda = 1$, $36.8\%$ of blocks are empty. Among non-empty blocks,
the size distribution is sharply concentrated on the smallest values:
\begin{itemize}
    \item $\Pr[X = 1 \mid X \geq 1] = e^{-1}/(1 - e^{-1}) \approx 58.2\%$
        (median of $1$ key);
    \item $\Pr[X \leq 2 \mid X \geq 1] \approx 87.3\%$;
    \item $\Pr[X \leq 3 \mid X \geq 1] \approx 97.0\%$ ($P_{90} = 3$).
\end{itemize}
That is, the local lookup is at most a $3$-element scan in $97\%$ of
non-empty buckets, with $58\%$ degenerating to a single comparison.

The classical balls-into-bins analysis~\citep[Lemma~5.1]{mitzenmacher2017probability}
bounds the maximum load by $3\ln n / \ln\ln n$ with probability $\geq 1 - 1/n$.
At $n = 2^{30}$ this gives $\approx 21$ keys per bucket.
Table~\ref{tab:ere-bucket-stats} reports the empirical maxima alongside the
Poisson reference $k^*$ --- the smallest $k$ with $B \cdot \Pr[\mathrm{Poisson}(1) \geq k] < 1$;
all measured maxima are well below the Lemma~5.1 bound.
Each observed max is the largest bucket over a single realisation of $n$ uniform
random 64-bit keys, taken across all $B$ blocks.

\begin{table}[h]
    \centering
    \begin{tabular}{@{}rrrrrrr@{}}
        \toprule
        $n$ & empty (\%) & observed max & $k^*$ & $B \cdot \Pr[X \geq k^*]$ & $w$ & $k^*$-bucket footprint (B) \\
        \midrule
        $2^{20}$ & 36.75 & 9  & 10 & 0.12 & 44 & 55 \\
        $2^{24}$ & 36.79 & 9  & 11 & 0.17 & 40 & 55 \\
        $2^{27}$ & 36.79 & 10 & 12 & 0.11 & 37 & 56 \\
        $2^{30}$ & 36.79 & 13 & 12 & 0.89 & 34 & 51 \\
        \bottomrule
    \end{tabular}
    \caption{Bucket statistics for $n$ keys in $B = n$ bins ($\lambda = 1$),
        assuming a full $64$-bit universe ($K = 64$).
        $k^*$: smallest $k$ with $B \cdot \Pr[\mathrm{Poisson}(1) \geq k] < 1$.
        $w = K - \lfloor \log_2 n \rfloor$ is the suffix width.
        The last column gives the bit-packed size of a bucket of $k^*$ keys
        ($k^* w / 8$ bytes, rounded up). In ARE deployments, $K$ and hence
        $w$ are typically smaller, so the resulting buckets are even more
        compact.}
    \label{tab:ere-bucket-stats}
\end{table}

The Poisson PMF $\Pr[X = k] = e^{-\lambda}\lambda^k/k!$~\citep[eq.~(6.1)]{feller1968introduction}
makes the tail decay like $1/k!$: even at $B = 2^{30}$, the expected number of
blocks with $\geq 20$ keys is ${\sim}1.7 \times 10^{-10}$, making buckets larger
than ${\sim}20$ keys virtually impossible for uniform data.

\paragraph{Non-uniform distributions and worst-case bounds}

For uniform data the previous paragraph showed that bucket sizes
concentrate around $\lambda = 1$: empirical maxima stay below
${\sim}13$ keys even at $n = 2^{30}$
(Table~\ref{tab:ere-bucket-stats}), regardless of $n$.

Real-world data is rarely uniform.
The original ARE construction~\citep{goswami2015approximate} uses a \emph{locality-preserving}
hash: consecutive input keys map to consecutive hashed values, so dense
input regions stay dense in the hashed universe.
On heavily clustered benchmarks the heaviest bucket reaches ${\sim}6 \times 10^4$~keys.
At the typical Facebook UDB range ($\mathcal{L} = 16$), median bucket
sizes stay in the low thousands; near production P90 ($\mathcal{L} = 256$)
they grow into the tens of thousands
(Table~\ref{tab:eval-bucket-fill}, Chapter~\ref{chap:evaluation}).
The worst-case footprint stays at ${\sim}120$~KB --- comfortably
L2-cache-resident.

Regardless of distribution, the bucket size is hard-bounded above by
the block capacity $2^w$, where $w = K - \lfloor \log_2 n \rfloor$ is
the suffix length: each bucket physically holds at most $2^w$ distinct
$w$-bit suffixes.
The choice of $w$ is constrained by the overall
filter size: the total ARE filter occupies approximately
$n \cdot (w + 2)$ bits ($w$ bits of suffix data plus ${\sim}2$~BPK of
metadata).
Compared to honest storage of 64-bit keys ($64 n$ bits),
the filter is meaningfully smaller only for $w$ well below $64$.
In published range filter evaluations the total per-key budget falls
in the $10$--$20$~BPK range, with $14$--$16$~BPK as the typical
headline configuration (Table~\ref{tab:industry-bpk},
Chapter~\ref{chap:evaluation}).
After subtracting the ${\sim}2$~BPK of rank/select metadata, this
corresponds to a suffix width $w \in [12, 15]$~bits in our pipeline.
A side benefit of $w = 16$ is that 16-bit suffixes align with SIMD
lanes for vector-friendly comparisons.

At $w = 15$ the hard upper bound is $2^{15} = 32\,768$~keys per bucket.
On a fully packed bucket of $2^w$ keys binary search terminates in at most
$\log_2(2^w) = w$ iterations --- and at $w = 15$ this still costs only
${\sim}48$~ns on Apple~M4~Max, comparable to a single Rank/Select on the metadata bit
vector.
The bound is distribution-independent and depends only on $w$,
not on $n$.
In real workloads observed buckets are typically two orders of
magnitude smaller than this hard limit; the worst case is bounded
but rare.

By the way, distributions with dense ranges can be handled by the hybrid filter of
Chapter~\ref{chap:hybrid}: each cluster is isolated into its own exact
sub-filter, turning local density into a compression advantage.

\paragraph{Search strategy.}
We implement both binary search $O(\log k)$ and linear scan $O(k)$ over
the $k$ suffixes of a bucket, and pick at query time based on $k$.
Apple~M4~Max benchmarks on uniform random $w$-bit suffixes:
linear scan is fastest for small buckets (${\sim}3$~ns at $k = 12$,
${\sim}5$~ns at $k = 24$, ${\sim}10$~ns at $k = 64$), while binary search
stays cheap even for pathologically large buckets (${\sim}40$~ns at
$k = 2^{12}$, ${\sim}60$~ns at $k = 2^{17}$) --- logarithmic growth
compresses the worst case to tens of nanoseconds.

The default adaptive dispatch (linear for $k \leq 128$, binary above) gives
the best of both branches: near-optimal in the common case and graceful
degradation on adversarial distributions.

For uniform 64-bit keys with non-empty queries, the total ERE query takes
${\sim}33$--$36$~ns on Apple~M4~Max, stable across $n \in [2^{20},\, 2^{28}]$.
Bucket search contributes only ${\sim}3$~ns at the typical $k \approx 12$;
the remainder is Rank/Select navigation on the metadata bit vectors.

\subsection{Weak Prefix Search}
\label{sec:wps}

\paragraph{Definition.}
A \emph{weak prefix query} on a sorted set $S' \subseteq U'$ is
specified by a bit string $p$ of length at most $\log_2 |U'|$ and
returns the interval of ranks of points in $S'$ whose binary
representation has $p$ as a prefix~\citep[\S 3.2]{goswami2015approximate}.
If no such points exist, the answer is arbitrary --- hence
\emph{weak}. The supporting index answers the query in $O(1)$
time in the word-RAM model.

\paragraph{Use in ERE.}
Given a query range $[a, b]$ inside a single block, the original
construction~\citep[\S 3.2]{goswami2015approximate} reduces local
emptiness to two weak prefix queries. Let $p$ be the longest common
prefix of the binary representations of $a$ and $b$ (an $O(1)$
most-significant-bit operation). Then $S' \cap [a, b] \neq \emptyset$
iff at least one of the following holds:
\begin{itemize}
    \item the largest point in $S'$ prefixed by $p \cdot 0$ exists
    and is $\geq a$, or
    \item the smallest point in $S'$ prefixed by $p \cdot 1$ exists
    and is $\leq b$.
\end{itemize}
Each side is one WPS query plus a comparison, giving $O(1)$ local
queries. The index is realised as a Z-Fast trie keyed by a Monotone
Minimal Perfect Hash Function~\citep{belazzougui2009monotone}.

\paragraph{Why we don't use it.}
The $O(1)$ guarantee is in the word-RAM model where hash table
lookups and trie navigation cost $O(1)$ and space is expressed up
to $O(n)$ additive terms. The hidden constants dominate in
practice.

We implemented three WPS variants, pairing a Z-Fast trie (standard or
succinct) with a Monotone Minimal Perfect Hash Function (MMPHF; fast or
compressed):

\begin{table}[h]
    \centering
    \begin{tabular}{@{}lrrrrrr@{}}
        \toprule
                                                & Space   & \multicolumn{5}{c}{Query latency (ns) at $n =$} \\
        \cmidrule(lr){3-7}
        Variant                                 & (bits/key) & $2^{10}$ & $2^{13}$ & $2^{15}$ & $2^{18}$ & $2^{20}$ \\
        \midrule
        Baseline (standard trie + fast MMPHF)   & 179        & 307      & 515      & 557      & 580      & 575      \\
        Compact (succinct trie + fast MMPHF)    & 151        & 490      & 681      & 712      & 751      & 812      \\
        Learned (succinct trie + learned MMPHF) & 59         & 544      & 683      & 758      & 787      & 889      \\
        \bottomrule
    \end{tabular}
    \caption{Three WPS variants on Apple~M4~Max, $K = 64$, 64-bit keys.
        Query latency generally rises with $n$ because the trie deepens,
        so each query traverses more levels and pays one
        hash-probe-and-pointer-chase per level. Raw bench output:
        \texttt{research/wps/locators/wps\_query\_latency\_2026-05-03.txt}.
        \colorbox{orange}{TODO: double-check before submission. The Space~(bits/key)
            figures predate the current code; the Baseline plateau after
            $n = 2^{15}$ also wants a re-measurement.}}
    \label{tab:wps-cost}
\end{table}

Even the most compact variant costs 59~BPK --- and this is an auxiliary
index \emph{on top of} the packed suffix array, which WPS accelerates but
does not replace. In our use case (ERE inside an ARE
filter, Section~\ref{sec:soda}), the suffix width $w$ is typically
$7$--$13$ bits per key depending on the target query range length $\mathcal{L}$, so WPS adds $5$--$8\times$ more space than the
suffix data it indexes.

Query latency is equally problematic. WPS replaces only the local search
inside a bucket; Rank/Select navigation on the metadata bit vectors
(${\sim}27$~ns) is identical for both approaches.
Table~\ref{tab:wps-vs-binsearch} compares the bucket-search step alone
across representative bucket sizes.

\begin{table}[h]
    \centering
    \begin{tabular}{@{}lrrr@{}}
        \toprule
        Bucket size $k$                  & Binary search (ns) & WPS Compact (ns) & Speedup            \\
        \midrule
        ${\sim}12$ (typical, uniform)    & $5$--$9$           & $490$--$812$     & $54$--$160\times$  \\
        $2^{12}$ ($k \approx 4{,}000$)   & ${\sim}40$         & $490$--$812$     & $12$--$20\times$   \\
        $2^{17}$ ($k \approx 131{,}000$) & ${\sim}60$         & $490$--$812$     & $8$--$14\times$    \\
        \bottomrule
    \end{tabular}
    \caption{Bucket-search latency on Apple~M4~Max, $K = 64$, 64-bit keys.
    WPS Compact numbers are the full range over $n \in \{2^{10}, \ldots, 2^{20}\}$
    from Table~\ref{tab:wps-cost}; binary search numbers are from the
    bucket-scan benchmark of Section~\ref{subsec:ere-local-search}.
    WPS pays a large per-call constant from trie pointer-chasing and hash
    probing that does not amortise with $k$, while binary search on packed
    suffixes scales as $O(\log k)$ with small constants on contiguous memory.
    \colorbox{orange}{TODO: double-check together with Table~\ref{tab:wps-cost}
        before submission --- the bucket-size axis here is conceptually
        independent of the $n$ axis used to derive the WPS Compact range.}}
    \label{tab:wps-vs-binsearch}
\end{table}

Even on adversarial bucket sizes WPS stays at least $8\times$ slower for
the operation it replaces.

The root cause is that the $O(1)$ theoretical guarantee hides large constants:
each ``constant-time'' trie step hashes a variable-length prefix, probes a
hash table, and chases a pointer. The probe and the pointer chase are random
memory accesses. A WPS query traverses several trie levels and pays this
cost on each, whereas a linear scan over a ${\leq}13$-element bucket
(${\leq}55$~B at $K = 64$, Table~\ref{tab:ere-bucket-stats}) touches a
single cache line. Binary search on packed suffixes also requires
no auxiliary index, whereas the most compact WPS variant adds $59$~BPK on
top of the suffix data.

\subsection{Space Analysis}

For $K$-bit keys, ERE stores $w = K - \lfloor \log_2 n \rfloor$ bits of
suffix per key plus ${\sim}2.53$~BPK of metadata on the one-vector
layout (single bit vector $D$ with rank/select indices,
Section~\ref{subsec:ere-one-vector}; physical figure from
Table~\ref{tab:ere-metadata-bpk}, stable across $n$).
At $K = 64$ and $n = 2^{28}$ ($w = 36$) this gives ${\sim}39$~BPK total
--- a ${\sim}39\%$ reduction over honestly storing the full 64-bit keys
($64$~BPK), but still ${\sim}2.5\times$ above the $14$--$16$~BPK budget
of industrial range filters
(Table~\ref{tab:industry-bpk}, Chapter~\ref{chap:evaluation}).

Space grows linearly with $K$ --- unavoidable for an exact structure,
since it must store the information distinguishing the keys.
This linear dependence motivates the move to approximate range emptiness:
by storing hashed fingerprints of length
$K \approx \log_2(\mathcal{L}/\varepsilon)$ instead of full keys, the
space becomes independent of the original key length.

\subsection{Complexity Summary}

Here $W$ denotes the machine word size.

\begin{itemize}
    \item \textbf{Build:} $O(n \cdot K)$ --- a single pass over sorted keys.
    \item \textbf{Query:}
    \subitem \emph{Expected} (uniform data): $O(\frac{K}{W})$ --- block index lookup
    dominates; bucket size is $O(1)$ on average
    (Section~\ref{subsec:block-sizes-and-lookup-strategy}).
    \subitem \emph{Worst case} (all keys collide in a single block):
    $O(\frac{K}{W} \log n)$.
    \subitem \emph{Inside ARE} with $K$-bit fingerprints fitting in one machine
    word ($K \leq W$): reduces to $O(1)$ expected.
    \item \textbf{Space:} $n (K - \log_2 n) + O(n)$ bits, matching the information-theoretic
    lower bound.
\end{itemize}
