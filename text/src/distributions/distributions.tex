\section{Key Distributions in Practice}
\label{sec:distributions}

The theoretical bounds of Section~\ref{sec:bounds} hold for any key set.
In practice, keys in storage systems follow distributions shaped
by the application domain.

\subsection{Common Industrial Distributions}
\label{subsec:common-industrial-distributions}

Keys in LSM-tree storage systems typically exhibit one or more of the following patterns:

\paragraph{Sequential / near-sequential.}
Auto-incrementing primary keys, monotonic timestamps, log sequence numbers. Keys are
approximately evenly spaced with small gaps.
The key space is dense – the spread is much smaller than the universe.
Among the Search on Sorted Data (SOSD)~\citep{kipf2019sosd} datasets, Facebook (user IDs), Books (popularity keys), and Wiki
(edit timestamps) all fall into this category.

\paragraph{Clustered.}
Keys concentrate in a few dense regions separated by large gaps.
Event logs keyed by user ID prefix + timestamp cluster around active users; inactive users leave gaps.
Geospatial keys such as S2 cell IDs~\citep{s2geometry} cluster around populated areas, with sparse rural regions.
OpenStreetMap (OSM) cell IDs are a representative example: they span nearly the full 64-bit space,
yet most keys concentrate in a small fraction of it.

\paragraph{Uniform-like.}
UUIDs, cryptographic hashes, randomly generated tokens.
Keys are spread across the full key space with large, roughly equal gaps.

\subsection{SOSD Benchmark Datasets}
\label{sec:sosd}

For empirical evaluation, we use real-world datasets from the SOSD (Search on Sorted
Data) benchmark~\citep{kipf2019sosd}:

\begin{table}[H]
    \centering
    \begin{tabular}{@{}llp{0.68\textwidth}@{}}
        \toprule
        Dataset & $n$ (full) & Character \\
        \midrule
        \textbf{Facebook} & 200M &
        User IDs from Facebook's social graph (uint64). \\
        \textbf{Books} & 200M &
        Amazon book popularity keys (uint32). \\
        \textbf{Wiki} & 200M &
        Unix timestamps of Wikipedia article edits (uint64). \\
        \textbf{OSM} & 800M &
        Hierarchical cell IDs covering global OpenStreetMap data (uint64). \\
        \bottomrule
    \end{tabular}
    \caption{SOSD datasets used in our evaluation.
    Benchmarks in Chapter~\ref{chap:evaluation} use
    subsets of these datasets (e.g.\ $n = 2^{24}$)}
    \label{tab:sosd}
\end{table}

\begin{remark}
    All datasets are sorted and deduplicated on load.
    The Wiki dataset contains many repeated timestamps, so its effective $n$
    is smaller than the requested raw count.
\end{remark}

\begin{table}[H]
    \centering
    \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lrcrrrrrr@{}}
        \toprule
        Dataset  & $n$                   & $U$ [bits]    & P10               & P50               & P90               & max               & mean \\
        \midrule
        Facebook & ${\approx}200\text{M}$ & 37           & 2                 & 28                & 693               & $5.7\times10^{5}$ & 387 \\
        Wiki     & ${\approx}90\text{M}$  & 29           & 1                 & 1                 & 3                 & $1.7\times10^{5}$ & 3 \\
        Books    & ${\approx}200\text{M}$ & 31           & 1                 & 2                 & 9                 & $2.0\times10^{4}$ & 5 \\
        OSM      & 800M                   & 64           & $1.6\times10^{6}$ & $4.5\times10^{7}$ & $1.7\times10^{9}$ & $2.3\times10^{17}$ & $1.7\times10^{10}$ \\
        \bottomrule
    \end{tabular*}
    \caption{Consecutive-key gap statistics for SOSD datasets after sort and deduplication.
        $n$: effective key count.
        $U$: key range in bits ($\lfloor\log_2(\max-\min)\rfloor + 1$).
        Gaps are differences between adjacent sorted keys.}
    \label{tab:sosd-gaps}
\end{table}

\begin{figure}[H]
    \centering
    \begin{tabular}{@{}cc@{}}
        \includegraphics[width=0.48\textwidth]{hist_sosd_fb} &
        \includegraphics[width=0.48\textwidth]{hist_sosd_books} \\
        \includegraphics[width=0.48\textwidth]{hist_sosd_wiki} &
        \includegraphics[width=0.48\textwidth]{hist_sosd_osm} \\
    \end{tabular}
    \caption{Key density histograms for SOSD datasets (1000 bins, log-scale Y-axis).
        Relative density is the fraction of all keys falling in each bin,
        normalized so that a perfectly uniform distribution gives~1 everywhere.}
    \label{fig:sosd-histograms}
\end{figure}

\subsection{Synthetic Distributions}
\label{subsec:synthetic-distributions}

To isolate the effect of specific distribution properties, we also generate synthetic
key sets:

\begin{itemize}
    \item \textbf{Uniform:} keys drawn uniformly from $[0, 2^{60})$.
    Average gap $\approx 2^{60}/n$.
    No clustering.
    \item \textbf{Clustered:} a mixture of Gaussian clusters with controlled parameters
    (number of clusters, intra-cluster spread, inter-cluster gap).
    \colorbox{orange}{TODO: state the concrete parameter values used in the benchmarks
        (number of clusters, $\sigma$, inter-cluster gap) --- see
        \texttt{Thesis/testutils/distribution.go:GenerateClusterDistribution}.}
\end{itemize}

Together, the SOSD and synthetic datasets span the spectrum from near-sequential
(Facebook, Books, Wiki) through clustered (OSM) to structureless (uniform
random) --- allowing us to evaluate each filter design across the range of distributions
it may encounter in practice.

\subsection{An assumption on queries}
\label{subsec:assumption}

We assume that queries target regions of the key space where stored keys are dense,
rather than arbitrary empty regions.

\begin{assumption}
    Queries reflect the application state and therefore concentrate on
    regions that contain data.
\end{assumption}

Consequently, FPR in sparse regions of the key space matters less --- dense regions
dominate the effective FPR experienced by the application.

The SODA hash (Section~\ref{sec:soda}) ignores this assumption entirely --- it provides
worst-case guarantees for any distribution. The truncation hash
(Section~\ref{sec:truncation}) performs well only under uniformity. The hybrid approaches
(Chapter~\ref{chap:hybrid}) explicitly exploit it by isolating dense regions.

Section~\ref{sec:query-distribution} operationalizes this assumption
in the query workload.

\section{Query generation}\label{sec:query-distribution}

Queries are drawn from a three-bucket mixture that reflects
the assumption of Section~\ref{subsec:assumption}:

\begin{itemize}
    \item \textbf{50\% near-key.}
    A random stored key $k$ is chosen; the query start is drawn
    uniformly from $[k - 5L,\; k + 5L)$.
    Models application lookups that miss by a small margin.
    \item \textbf{30\% in-gap.}
    A random gap of size $\geq 2$ is selected uniformly; the query
    is placed inside it and truncated to the gap boundary if it
    overflows. Empty by construction on every dataset.
    Models misses in locally dense regions.
    \item \textbf{20\% uniform.}
    A random start is drawn uniformly from $[\min k, \max k]$.
    Covers sparse regions to verify FPR stays reasonable there too.
\end{itemize}

The near-key and in-gap buckets reflect the dominant workload;
the uniform bucket acts as a safety check on sparse regions.
The headline \emph{gap-heavy} workload of
Section~\ref{sec:eval-tables} reweighs the mixture to
$0\,/\,70\,/\,30$ (no near-key, 70\% in-gap, 30\% uniform) so that no
candidate query is guaranteed empty just by its construction.

The two metrics this thesis cares about --- FPR and query latency
--- want different things from the same query stream, so we draw
two query sets from the mixture above and let truncation decide
which.

\subsection{Empty queries for False Positive Rate Measurement}
\label{subsec:queries-empty}

For FPR estimation every measured query must be empty: a positive
answer on a non-empty query is a true positive, not a false one,
and would deflate the FPR estimate. We enforce emptiness by
\emph{truncation}: when a candidate query $[a, a+L)$ would contain
a stored key $k^*$, it is shortened to $[a, k^* - 1]$ rather than
discarded and resampled. Truncation preserves queries that start
inside a cluster and end between two closely spaced keys, which
would otherwise be rejected every time --- biasing the workload
towards cluster boundaries and missing the harder case of a query
landing deep inside a dense region. The effective range length
may therefore be smaller than $L$.

\subsection{Combined queries (for latency)}
\label{subsec:queries-combined}

In a deployed LSM-tree, the primary performance contribution of a filter
lies in its handling of empty queries. When a filter returns a
``not empty'' result, the storage engine must proceed with a block read,
an I/O operation whose cost typically dominates the filter's execution time.
Thus, the filter's latency is most critical when it successfully prevents
a disk fetch. For this reason, our primary latency evaluations are
conducted on the same empty-only query stream used for FPR measurement.

Nevertheless, it is still necessary to ensure that filter performance does
not degrade severely on non-empty queries. If a filter's ``not empty''
evaluation path were significantly slower than its ``empty'' path, it could
introduce noticeable CPU overhead before the I/O operation even begins.
To evaluate this, we generate a second query stream using the same mixture,
but omit the forced truncation step (while retaining in-gap truncation).
In this stream, a query that overlaps with a stored key is evaluated at
its full length $L$ rather than being artificially shortened to $[a, k^* - 1]$.

The non-empty fraction depends on the local key density and on
$L$. Under the smart-mix weights ($50\,/\,30\,/\,20$) the $30\%$
in-gap queries remain empty by construction; the other two
buckets contribute non-empty queries as follows.
The $50\%$ near-key bucket draws an offset uniformly from
$[-5L, 5L)$, so even on a sparse universe roughly
$\tfrac{1}{10}$ of these queries land on the reference key ---
a $\sim\!5\%$ non-empty floor that is independent of data density.
On distributions where the average gap between adjacent keys is
smaller than $L$, near-key queries also catch one or more
neighbours and the bucket essentially saturates near its $50\%$
limit.
The $20\%$ uniform bucket adds about $L \cdot n / U$ on top, which
matters on small-universe inputs (Facebook, Wiki) and is
negligible on $64$-bit universes (OSM, Books-800M, Uniform).
\colorbox{orange}{TODO: Books-800M is a separate 64-bit Books variant; clarify or replace with OSM only.}
The ceiling is therefore $70\%$ when both buckets always hit.
Table~\ref{tab:nonempty-fraction} reports the measured fractions
at $n = 2^{28}$ across the datasets used in
Section~\ref{sec:eval-tables}.

\begin{table}[H]
    \centering
    \begin{tabular}{@{}lrrr@{}}
        \toprule
        Dataset & $L=1$ & $L=16$ & $L=1024$ \\
        \midrule
        Facebook  & $9.0\%$  & $20.7\%$ & $50.6\%$ \\
        Wiki      & $46.3\%$ & $63.2\%$ & $68.4\%$ \\
        Books     & $5.0\%$  & $5.0\%$  & $4.9\%$  \\
        OSM       & $5.0\%$  & $5.0\%$  & $4.9\%$  \\
        Uniform   & $5.0\%$  & $5.0\%$  & $4.9\%$  \\
        Clustered & $4.2\%$  & $6.8\%$  & $9.7\%$  \\
        \bottomrule
    \end{tabular}
    \caption{Non-empty fraction of the combined-queries workload
        under the smart-mix weights ($n = 2^{28}$). Wiki has
        average inter-key gap close to $1$, so its near-key bucket
        is essentially fully non-empty already at $L = 1$. The
        other dense distribution, Facebook, picks up the
        uniform-bucket contribution as $L$ grows. The
        large-universe datasets (OSM, Books-800M, Uniform) sit at
        the $5\%$ near-key floor; the synthetic Clustered set is
        only mildly denser.
        \colorbox{orange}{TODO: confirm Books-800M variant or replace with OSM only.}}
    \label{tab:nonempty-fraction}
\end{table}
