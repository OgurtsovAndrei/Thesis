\begin{frame}[c]{Succinct Backend}
    \textbf{The Exact Range Emptiness (ERE) backend underneath every Approximate Range filter.}
    \medskip
    \begin{table}
        \centering
        \footnotesize
        \setlength{\tabcolsep}{4pt}
        \begin{tabular*}{0.95\textwidth}{@{\extracolsep{\fill}}lccc@{}}
            \toprule
            & \textbf{Goswami baseline}          & \textbf{Ours}                                  & \textbf{Improvement}                 \\
            \midrule
            Navigation      & 2 bitvectors $(D_1, D_2)$          & 1 bitvector $D$                       & $1.21$--$3.00\times$ faster \\
            Metadata size    & $3.33$ bits/key                    & $2.53$ bits/key                       & $-0.80$ bits/key ($\sim 24\%$)            \\
            Bucket search   & Weak Prefix Search (WPS) & Binary / linear search & $8$--$160\times$ faster     \\
            Bucket index    & $\ge 59$ BPK auxiliary index & None --- in-place scan & $\ge 59$ BPK saved          \\
            \bottomrule
        \end{tabular*}\label{tab:ere-table}
    \end{table}
    \medskip
    \begin{itemize}
        \setlength{\itemsep}{0.4em}
        \item \textbf{Worse asymptotics, faster in practice:} on small buckets --- the common case
        in real workloads --- $O(\log k)$ binary search or even $O(k)$ linear scan beats Weak Prefix
        Search's $O(1)$, because that constant hides hash probes and pointer chases at every trie level.
    \end{itemize}
\end{frame}